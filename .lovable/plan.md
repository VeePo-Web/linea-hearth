Remove the "+100,000 happy customers" item (with Users icon) from the rotating status bar in `src/components/header/StatusBar.tsx`. Also drop the now-unused `Users` import from `lucide-react`. The bar will rotate through 3 items: free shipping, 365-day warranty, free returns.